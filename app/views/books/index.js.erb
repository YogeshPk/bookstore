$('.table').append('<%= j render @books %>');
<% if @books.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @books %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>