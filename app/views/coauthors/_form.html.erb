<%= form_for(coauthor) do |f| %>
  <% if coauthor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coauthor.errors.count, "error") %> prohibited this coauthor from being saved:</h2>

      <ul>
      <% coauthor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :book_id %>
    <%= f.text_field :book_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
